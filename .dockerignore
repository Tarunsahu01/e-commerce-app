# Git and IDE
.git
.gitignore
.cursor
*.md

# Frontend (not needed for backend image)
frontend/node_modules
frontend/dist

# Backend build output
target

# Local uploads / runtime (recreated in container or via volume)
uploads

# Root-level stray node_modules if any
node_modules

# Binaries / OS
bin
*.class
.DS_Store
Thumbs.db
